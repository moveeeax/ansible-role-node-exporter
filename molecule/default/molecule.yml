---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: node-exporter-el8
    image: "geerlingguy/docker-centos8-ansible:latest"
    command: /usr/sbin/init
    privileged: true
    pre_build_image: true
provisioner:
  name: ansible
verifier:
  name: ansible
