---
- name: Converge
  hosts: all
  become: true
  roles:
    # Molecule installs the role under its fully qualified Galaxy name (built
    # from meta/main.yml namespace + role_name), which resolves regardless of
    # what the checkout directory happens to be called.
    - role: moveeeax.node_exporter
